{
  "name": "curve-similarity-toy",
  "packageManager": "yarn@4.0.0-rc.33",
  "scripts": {
    "build:wasm": "wasm-pack build rs_lib --target web",
    "build": "node build.mjs",
    "watch": "yarn build --watch",
    "serve": "esbuild --servedir=www",
    "test:types": "tsc"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.9",
    "esbuild": "^0.16.1",
    "tsc": "^2.0.4",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
